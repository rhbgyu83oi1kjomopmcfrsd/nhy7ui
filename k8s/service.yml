apiVersion: v1
kind: Service
metadata:
  name: bkhy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bkhy"
    port: 8080
  selector:
    app: bkhy
